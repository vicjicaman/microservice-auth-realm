apiVersion: v1
kind: Service
metadata:
  name: "${ACCOUNT_GRAPH_SERVICE_NAME@microservice-account-config}"
  namespace: "microservices-namespace"
  labels:
    moduleid: microservice-auth-realm
    version: 1.70.4-master
spec:
  type: ClusterIP
  selector:
    app: "auth-account-graph-deployment"
  ports:
    - name: port
      port: "${ACCOUNT_GRAPH_INTERNAL_PORT@microservice-account-config}"
